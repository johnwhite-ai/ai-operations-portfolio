# Knowledge & Memory

The institutional memory. This department is why the system learns instead of decaying.

- `knowledge-curator` — captures lessons, refines agents
- `memory-keeper` — recalls history and context
