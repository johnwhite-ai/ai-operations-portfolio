"""ops-profiler — turn raw, messy operational data into a structured, fully
sourced profile with confidence on every value and every gap flagged.

Pipeline: ingest -> extract -> profile -> report
Deterministic core (ingest, math, provenance); AI-assisted edges (fuzzy column
mapping, unstructured text), always checked against the canonical schema.
"""
